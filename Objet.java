package triSelectifACompleter;

// Représente un objet dans le monde (déchet ou particule).
public class Objet {
	// TODO GRETA : la position de l'objet (en 2D) dans l'environnement : 2 lignes.
    
    // Constructeur.
    public Objet() {
    }
    
    // Constructeur.
    public Objet(double _x, double _y) {
    	// TODO GRETA : 2 lignes : initialisation de la position de l'objet.
    }
    
    // Permet de calculer la distance entre l'objet courant et celui passé en paramètre.
    public double Distance(Objet o) {
        // TODO GRETA : le 0 doit être remplacé par le calcul de la distance.
    	return 0;
    }
    
    // Permet de calculer la distance au carré (évite de calculer la racine carrée qui est
    // coûteuse en termes de performances).
    public double DistanceCarre(Objet o) {
        // TODO GRETA : le 0 doit être remplacé par le calcul de la distance.
        return 0;
    }
}
